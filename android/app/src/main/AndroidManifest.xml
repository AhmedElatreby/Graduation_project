<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elatreby.safety">

    <!-- Kept to the minimum the app actually uses:
         FINE_LOCATION  - live tracking + SOS location link
         WAKE_LOCK      - siren keeps playing with the screen off (audioplayers stayAwake)
         SEND_SMS       - emergency SMS via telephony
         INTERNET/NETWORK_STATE - Firebase + Google Maps
         (READ_SMS, READ/WRITE_CONTACTS, ACCESS_BACKGROUND_LOCATION were
         declared but never used; the SMS/contacts ones are Play-restricted.) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Background shake-to-SOS (Android only):
         POST_NOTIFICATIONS          - countdown + result notifications (API 33+)
         FOREGROUND_SERVICE(+type)   - keeps shake detection alive in background
         CALL_PHONE                  - direct-dial the first guardian
         VIBRATE                     - countdown ticks are felt, not just seen -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 10+ only serves while-in-use GPS to a foreground service
         that declares the location type; without it the backgrounded SOS
         SMS always says "location unavailable". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.VIBRATE" />


    <application
        android:label="Safety App"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
       <!-- api key -->
       <meta-data android:name="com.google.android.geo.API_KEY"
           android:value="AIzaSyBFWjw90iH5rkDve37AUtJ67BZVk1XQAaI"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
       <meta-data android:name="com.google.android.geo.API_KEY"
           android:value="AIzaSyBFWjw90iH5rkDve37AUtJ67BZVk1XQAaI"/>
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location|specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Personal-safety shake detection with a user-cancellable SOS countdown" />
        </service>
    </application>
    <queries>
        <!-- If your app makes calls -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>
